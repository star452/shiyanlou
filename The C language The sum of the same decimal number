



#include<stdio.h>

void main()
{
	int  j,d,n;   //n项
	double t, s;

	printf("请输入整数d,n:");
	scanf("%d%d", &d, &n);

	t = 1; s = 0;
	for (j = 1; j <=n; j++)
	{
		t = t/ 10;
		s = s+(1 - t) / 9 * d;
	}
	printf("  s(%d,%d)=%.8f\n", d, n, s);

}
